/**
 * Beranda Layout
 *
 * Header dan footer di-handle oleh GlobalAppShell
 * Providers fetching di-handle oleh GlobalAppShell
 */

export default function BerandaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
